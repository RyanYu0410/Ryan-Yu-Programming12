package bonusUnfinished;

public class Message {

    public void setMessage(String message) {
        this.message = message;
    }
    String message;
    public Message() {
        this.message = "Sets only will add on unique Objects.  Once the above is deal with just add each word.  If it is unique it will be added if it is not unique it will not be added.\n";
    }
}
